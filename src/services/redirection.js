export const RedirectToHome = (props) => {
  // if admin redirect to admin home page
  // if user redirec to user home

}

export const RedirectToLogin = (props) => {
  // find appropriate login url and ridirect
}

// 